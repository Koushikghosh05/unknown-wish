{
  "name": "unknown-wishing-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node server.js",
    "start": "npm run build && node server.js"
  },
  "dependencies": {
    "@vercel/kv": "^0.2.4",
    "express": "^4.18.2",
    "redis": "^5.12.1"
  },
  "devDependencies": {
    "vite": "^6.2.2"
  }
}
